# infra

- `docker-compose.yml` — Postgres + pgvector **local** (alternativa opcional ao
  Supabase gerenciado). O padrão do projeto é Supabase em `sa-east-1`.

IaC (Vercel/Railway/Fly) entra em estágios posteriores.
